<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Modifier Utilisateur</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <header>
    <h1>Modifier l'Utilisateur</h1>
    <nav>
        <!-- Bouton Burger (visible uniquement sur PC) -->
        <div class="burger-menu" id="burger-menu">
            <span></span>
            <span></span>
            <span></span>
        </div>
    
        <!-- Menu qui sera caché par défaut (mobile) et affiché lors du clic sur le bouton burger -->
        <ul class="nav-links" id="nav-links">
            <li><a href="/dashboard">Tableau de Bord</a></li>
            <li><a href="/catways">Gestion des Catways</a></li>
            <li><a href="/reservations">Gestion des Réservations</a></li>
            <li><a href="/users">Gestion des Utilisateurs</a></li>
            <li><a class="API-document" href="/api-docs">Documentation API</a></li>
            <li><a href="/logout" class="logout-button">Se Déconnecter</a></li>
        </ul>
    </nav>
    </header>
    <form method="POST" action="/users/<%= user._id %>?_method=PUT">
        <label>
            Nom d'utilisateur :
            <input type="text" name="username" value="<%= user.username %>" required minlength="3">
        </label>
        <br>
        <label>
            Adresse email :
            <input type="email" name="email" value="<%= user.email %>" required>
        </label>
        <br>
        <button type="submit">Enregistrer les modifications</button>
    </form>
    <a href="/users" class="button red">Annuler</a>
    <script>
        // Sélection des éléments HTML
        const burgerMenu = document.getElementById("burger-menu");
        const navLinks = document.getElementById("nav-links");
    
        // Ajout d'un événement de clic pour ouvrir/fermer le menu et animer le burger
        burgerMenu.addEventListener("click", () => {
            navLinks.classList.toggle("active");
            burgerMenu.classList.toggle("open"); // Ajoute la classe "open" pour l'animation
        });
    </script>
    

</body>
</html>
